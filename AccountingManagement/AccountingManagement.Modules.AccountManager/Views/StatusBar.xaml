<UserControl x:Class="AccountingManagement.Modules.AccountManager.Views.StatusBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AccountingManagement.Modules.AccountManager.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="800">
    <Border BorderBrush="LightGray" BorderThickness="0,1,0,0">
        <DockPanel LastChildFill="True">
            <StackPanel DockPanel.Dock="Left" Height="20">
                <TextBlock Margin="5,0" Text="{Binding LoggedInDisplayName, UpdateSourceTrigger=PropertyChanged, StringFormat={}Logged in as {0}}" />
            </StackPanel>
            <StackPanel HorizontalAlignment="Right">
                <TextBlock Margin="10,0" Text="{Binding VersionText}" />
            </StackPanel>
        </DockPanel>
    </Border>
</UserControl>
